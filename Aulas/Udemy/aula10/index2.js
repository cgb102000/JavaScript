function soma (x=1, y=1) {
    const resultado = x + y;
    return resultado; 
    console.log(`Teste`);    //Tudo que fica abaixo do return não é considerado
}
const resultado = (soma());
console.log(resultado)